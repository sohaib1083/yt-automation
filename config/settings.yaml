video:
  width: 1920
  height: 1080 # 16:9 — standard YouTube landscape (most watched format)
  shorts_width: 1080
  shorts_height: 1920 # 9:16 — vertical for YouTube Shorts
  fps: 30 # 30fps for smooth playback
  codec: libx264
  bitrate: '6000k' # higher bitrate for crisp 1080p

tts:
  # Provider priority: ElevenLabs → Edge-TTS → Deepgram → gTTS
  # ElevenLabs (used when ELEVENLABS_API_KEY is set in .env)
  elevenlabs_voice_id: '21m00Tcm4TlvDq8ikWAM' # Rachel — warm, clear female narrator
  elevenlabs_model: eleven_multilingual_v2
  # Edge-TTS — Microsoft neural voices, FREE, no API key needed
  # Options: en-US-AndrewNeural, en-US-ChristopherNeural, en-US-BrianNeural,
  #          en-US-AriaNeural, en-US-JennyNeural, en-US-EricNeural
  edge_voice: en-US-AndrewNeural # warm authoritative male narrator
  # Deepgram (used when DEEPGRAM_API_KEY is set in .env)
  # gTTS — last resort fallback
  lang: en

pexels:
  per_page: 5 # Fetch 5 candidates, pick first landscape
  orientation: landscape
  min_width: 1920

script:
  sections: 6 # Number of video sections to generate
  words_per_section: 80 # ~30 seconds of narration per section

youtube:
  channel_name: 'AI Frontiers'
  category_id: '28' # 28 = Science & Technology (best for AI content)
  privacy_status: public
  made_for_kids: false
  notify_subscribers: true
